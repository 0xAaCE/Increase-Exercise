{
  "name": "bot",
  "version": "1.0.0",
  "description": "The bot will request the info to the Increase API and save into de DB",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --inspect index.js",
    "migrations": "echo 'Migrated'",
    "test": "NODE_ENV=testing jest test/app --runInBand --forceExit --detectOpenHandles"
  },
  "author": "aleacevedo",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.0",
    "dotenv": "^8.2.0",
    "forever": "^1.0.0",
    "logger": "0.0.1",
    "moment": "^2.24.0",
    "node-schedule": "^1.3.2",
    "nodemon": "^1.19.4"
  },
  "devDependencies": {
    "jest": "^24.9.0"
  }
}
